Proposed patches (no cambios en archivos originales)

Resumen:
- Se generaron copias en `proposed_patches/` para revisión antes de aplicar.
- Cambios realizados en las copias:
  - Añadido/extendido `meta name="description"` cuando la descripción era insuficiente (<50 caracteres).
  - Añadido `link rel="canonical"` cuando faltaba.
  - Insertado `script type="application/ld+json"` (JSON-LD) mínimo para páginas clave (WebPage o resumen de LocalBusiness/Restaurant).

Archivos incluidos en esta entrega inicial:
- mapa-de-calarca-2026.html
- mapa-de-circasia-2025.html
- mapa-del-quindio.html
- top-pueblos.html
- alquiler-finca-hoteles.html
- negocios/buen-vuelo-tours.html
- negocios/cantina-el-desmadre.html

Próximos pasos sugeridos:
1. Revisa cada archivo en `proposed_patches/` y confirma si quieres aplicar los cambios.
2. Si todo está OK, puedo aplicar los cambios a los archivos originales (con respaldo) o ajustar textos (titulares o snippets) según tu preferencia.
3. Opcional: generar parches para el resto de páginas con warnings (titles largos, más JSON-LD) en la misma carpeta.

Notas:
- No se cambió la estructura visual ni el contenido del body salvo en `alquiler-finca-hoteles.html` donde añadí JSON-LD y un comentario indicando que el resto del body se mantiene sin cambios.
- Las copias están listas para comparación y revisión.
